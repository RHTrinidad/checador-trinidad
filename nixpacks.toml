toml[phases.setup]
nixPkgs = ["nodejs_20", "chromium"]
aptPkgs = ["libgbm1", "libnss3", "libatk-bridge2.0-0", "libgtk-3-0", "libasound2"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node index.js"
